import styled from 'styled-components'

const Main = styled.main`
  color: ${({ theme }) => theme.fgColor};
  font: 1em sans-serif;
  margin: auto;
  max-width: 38em;
  padding: 0 1em;
`
export default Main
